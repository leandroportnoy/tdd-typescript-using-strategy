enum PlansLimitsTypes {
    DEFAULT = "",
    LOW = "low",
    MEDIUM = "medium",
    NORMAL = "normal",
    RENOVATION = "renovation",
    NONRENOVATION = "nonrenovation"

    // DEFAULT = 0,
    // LOW = 20 >> 20 && 20 << 40,
    // MEDIUM = 40 >> 40 && 20 << 60,
    // NORMAL = 60 >> 60,
    // RENOVATION = 10,
    // NONRENOVATION = 100
}

export default PlansLimitsTypes;